% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slp_gas_reference.R
\name{slp_gas_coefficients}
\alias{slp_gas_coefficients}
\title{Retrieve SigLinDe Coefficients for Gas Standard Load Profiles}
\source{
BDEW/VKU/GEODE (2025). \emph{Leitfaden Abwicklung von Standardlastprofilen
Gas}, Kooperationsvereinbarung Gas, Annex XIV.2, as of 2025-10-28,
Appendix 6.
\url{https://www.bdew.de/media/documents/251028_LF_SLP_Gas_KoV_XIV.2.pdf}
}
\usage{
slp_gas_coefficients(profile_id = NULL, variant = NULL)
}
\arguments{
\item{profile_id}{character vector of gas profile identifiers. One or more
of \code{"HEF"}, \code{"HMF"}, \code{"HKO"}, \code{"GKO"}, \code{"GHA"}, \code{"GMK"}, \code{"GBD"},
\code{"GBH"}, \code{"GWA"}, \code{"GGA"}, \code{"GBA"}, \code{"GGB"}, \code{"GPD"}, \code{"GMF"},
\code{"GHD"}. Pass \code{NULL} (the default) to retrieve all 15 profiles.}

\item{variant}{character vector of SigLinDe variants to include. One or
both of \code{"34"} (57 \% linear component) and \code{"33"} (45 \% linear
component). Pass \code{NULL} (the default) to retrieve both variants.
Duplicate values are silently ignored.}
}
\value{
A data frame with one row per profile–variant combination and
11 variables:
\describe{
\item{profile_id}{character, gas profile identifier}
\item{variant}{character, SigLinDe variant (\code{"34"} or \code{"33"})}
\item{A, B, C, D}{numeric, sigmoid coefficients}
\item{theta0}{numeric, pole temperature (40 °C for all published profiles)}
\item{mH, bH}{numeric, slope and intercept of the space-heating line
(\emph{Heizgas-Gerade})}
\item{mW, bW}{numeric, slope and intercept of the hot-water line
(\emph{Warmwasser-Gerade})}
}
}
\description{
Returns the SigLinDe profile function coefficients for one or more gas
standard load profiles as a data frame. These are the values used
internally by \code{\link[=slp_gas]{slp_gas()}} and \code{\link[=slp_gas_siglinde]{slp_gas_siglinde()}}.
}
\details{
The \code{HKO} profile (Kochgasprofil) is a pure sigmoid with no linear
component; its \code{mH}, \code{bH}, \code{mW}, and \code{bW} are all zero for both variants.

The returned coefficients can be passed directly to \code{\link[=slp_gas_siglinde]{slp_gas_siglinde()}}
for custom calculations. When selecting a single profile and variant the
result is a one-row data frame, so use \verb{[[ ]]} or \code{$} to extract scalars:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{p <- slp_gas_coefficients("HEF", variant = "34")
slp_gas_siglinde(0, p$A, p$B, p$C, p$D, p$theta0, p$mH, p$bH, p$mW, p$bW)
}\if{html}{\out{</div>}}

[ ]: R:\%20
}
\examples{
# Single profile, both variants
slp_gas_coefficients("HEF")

# Single profile, single variant
slp_gas_coefficients("HEF", variant = "34")

# Both variants explicitly — same as NULL
slp_gas_coefficients(c("HEF", "GKO"), variant = c("34", "33"))
}
\seealso{
\code{\link[=slp_gas_siglinde]{slp_gas_siglinde()}}, \code{\link[=slp_gas]{slp_gas()}}, \code{\link[=slp_gas_weekday_factors]{slp_gas_weekday_factors()}};
all values are listed in tabular form in the
\href{https://flrd.github.io/standardlastprofile/articles/slp-gas-parameters.html}{SigLinDe parameters}
article.
}
